# Database Seeding Script

This script sets up a MySQL database named `ALX_prodev` with a table `user_data` and populates it with data from a CSV file.

## Requirements
- Python 3
- mysql-connector-python library
- MySQL server running locally

## Installation
1. Install the required package:
   ```bash
   pip install mysql-connector-python